version: "3.3"

services:
  ganache:
    image: trufflesuite/ganache-cli:v6.12.2
    command: --chainId 1337 -m "tonight victory turtle miss chalk width easily inform quote script melody proof"
    ports:
      - "8545:8545"
  
  ethereum:
    build:
      context: ethereum
      dockerfile: docker/deploy.Dockerfile
    entrypoint: sleep 7200
    depends_on: 
      - ganache
    environment:
      LEDGERHOSTNAME: ganache